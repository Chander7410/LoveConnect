map $http_upgrade $connection_upgrade {
  default upgrade;
  '' close;
}

server {
  listen ${PORT};
  server_name _;

  resolver 1.1.1.1 8.8.8.8 valid=30s ipv6=off;

  proxy_http_version 1.1;
  proxy_ssl_server_name on;
  proxy_set_header Host $host;
  proxy_set_header X-Real-IP $remote_addr;
  proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
  proxy_set_header X-Forwarded-Proto $scheme;

  location = /api/health {
    add_header X-LoveConnect-Service auth always;
    proxy_pass ${AUTH_SERVICE_ORIGIN};
  }

  location ^~ /api/auth {
    add_header X-LoveConnect-Service auth always;
    proxy_pass ${AUTH_SERVICE_ORIGIN};
  }

  location ^~ /api/profiles {
    add_header X-LoveConnect-Service profile always;
    proxy_pass ${PROFILE_SERVICE_ORIGIN};
  }

  location ^~ /api/profile {
    add_header X-LoveConnect-Service profile always;
    proxy_pass ${PROFILE_SERVICE_ORIGIN};
  }

  location /api/search {
    add_header X-LoveConnect-Service profile always;
    proxy_pass ${PROFILE_SERVICE_ORIGIN};
  }

  location /api/likes {
    add_header X-LoveConnect-Service profile always;
    proxy_pass ${PROFILE_SERVICE_ORIGIN};
  }

  location /api/notifications {
    add_header X-LoveConnect-Service profile always;
    proxy_pass ${PROFILE_SERVICE_ORIGIN};
  }

  location /api/subscriptions {
    add_header X-LoveConnect-Service profile always;
    proxy_pass ${PROFILE_SERVICE_ORIGIN};
  }

  location /api/safety {
    add_header X-LoveConnect-Service profile always;
    proxy_pass ${PROFILE_SERVICE_ORIGIN};
  }

  location ^~ /api/chats {
    add_header X-LoveConnect-Service chat always;
    proxy_pass ${CHAT_SERVICE_ORIGIN};
  }

  location ^~ /api/chat {
    add_header X-LoveConnect-Service chat always;
    proxy_pass ${CHAT_SERVICE_ORIGIN};
  }

  location ^~ /api/calls {
    add_header X-LoveConnect-Service call always;
    proxy_pass ${CALL_SERVICE_ORIGIN};
  }

  location ^~ /ws {
    add_header X-LoveConnect-Service call always;
    proxy_pass ${CALL_SERVICE_ORIGIN};
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
  }

  location / {
    return 404;
  }
}
